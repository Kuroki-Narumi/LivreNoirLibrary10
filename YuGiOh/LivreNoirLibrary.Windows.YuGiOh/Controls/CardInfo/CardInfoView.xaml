<UserControl x:Class="LivreNoirLibrary.Windows.YuGiOh.Controls.CardInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LivreNoirLibrary.Windows.YuGiOh.Controls"
             xmlns:ygo="clr-namespace:LivreNoirLibrary.Windows.YuGiOh"
             xmlns:ct="clr-namespace:LivreNoirLibrary.Windows.Controls;assembly=LivreNoirLibrary.Windows.Controls"
             xmlns:md="clr-namespace:LivreNoirLibrary.Windows.Media;assembly=LivreNoirLibrary.Windows.Media"
             xmlns:ygd="clr-namespace:LivreNoirLibrary.YuGiOh.Data;assembly=LivreNoirLibrary.YuGiOh.Core"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/LivreNoirLibrary.Windows.YuGiOh;component/Themes/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <Style TargetType="TextBox">
                <Setter Property="Margin" Value="2,2,0,2"/>
                <Setter Property="Padding" Value="2"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="Grid.Column" Value="2"/>
            </Style>
            <Style x:Key="TextBox_Text" TargetType="TextBox" BasedOn="{StaticResource TextBox_Scrollable}">
                <Setter Property="Margin" Value="0"/>
            </Style>
            <Style TargetType="Label" BasedOn="{StaticResource Label_Header}"/>
            <Style TargetType="TextBlock">
                <Setter Property="HorizontalAlignment" Value="Left"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="Margin" Value="4,0"/>
            </Style>
            <Style x:Key="TextBlock_TextLength" TargetType="TextBlock">
                <Setter Property="Background" Value="{ct:BrushProvider #8fff}"/>
                <Setter Property="VerticalAlignment" Value="Bottom"/>
                <Setter Property="HorizontalAlignment" Value="Right"/>
                <Setter Property="Margin" Value="0,0,1,1"/>
                <Setter Property="Padding" Value="2,0"/>
                <Setter Property="IsHitTestVisible" Value="False"/>
            </Style>
            <Style TargetType="ct:IconPresenter">
                <Setter Property="Height" Value="16"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>

            <Style x:Key="IconButtonBase" TargetType="ct:Button">
                <Setter Property="BorderBrush" Value="{x:Null}"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="CornerRadius" Value="0"/>
                <Setter Property="Margin" Value="0"/>
                <Setter Property="Padding" Value="0"/>
                <Setter Property="VerticalAlignment" Value="Stretch"/>
            </Style>
            <Style x:Key="CopyButton" TargetType="ct:Button" BasedOn="{StaticResource IconButtonBase}">
                <Setter Property="Background" Value="{StaticResource Brush_HeaderBackground}"/>
                <Setter Property="Icon" Value="{x:Static md:Icons.Copy}"/>
                <Setter Property="HorizontalAlignment" Value="Left"/>
                <Setter Property="Grid.Column" Value="1"/>
                <EventSetter Event="Click" Handler="OnClick_Copy"/>
            </Style>
            
            <Style x:Key="ListBox_Link" TargetType="ListBox" BasedOn="{StaticResource Alt2ListBoxStyle}">
                <Setter Property="Height" Value="{x:Static local:CardInfoView.DefaultPackListHeight}"/>
                <Setter Property="FontSize" Value="11"/>
                <Setter Property="SelectionMode" Value="Single"/>
                <Setter Property="Grid.Row" Value="1"/>
                <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
            </Style>
            <local:ExpanderStateConverter x:Key="ExpConv"/>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid RowDefinitions="Auto, *, 6, Auto" x:Name="MainGrid" d:DataContext="{d:DesignInstance Type=local:CardInfoViewModel}">
        <!-- card name -->
        <Grid Grid.Row="0" ColumnDefinitions="Auto, Auto, *" RowDefinitions="Auto, Auto, Auto">
            <Label Grid.Row="0" Content="{Binding CInfo.Name.Value, Source={x:Static ygo:Vocab.Current}}"/>
            <ct:Button Grid.Row="0" Style="{StaticResource CopyButton}" Tag="{Binding ElementName=TextBox_Name}"/>
            <TextBox Grid.Row="0" x:Name="TextBox_Name" Text="{Binding Card.Name, Mode=OneWay}"/>
            
            <Label Grid.Row="1" Content="{Binding CInfo.Ruby.Value, Source={x:Static ygo:Vocab.Current}}"/>
            <ct:Button Grid.Row="1" Style="{StaticResource CopyButton}" Tag="{Binding ElementName=TextBox_Ruby}"/>
            <TextBox Grid.Row="1" x:Name="TextBox_Ruby" Text="{Binding Card.Ruby, Mode=OneWay}"/>
            
            <Label Grid.Row="2" Content="{Binding CInfo.EnName.Value, Source={x:Static ygo:Vocab.Current}}"/>
            <ct:Button Grid.Row="2" Style="{StaticResource CopyButton}" Tag="{Binding ElementName=TextBox_EnName}"/>
            <TextBox Grid.Row="2" x:Name="TextBox_EnName" Text="{Binding Card.EnName, Mode=OneWay}"/>

            <ct:Button Grid.Column="2" Style="{StaticResource IconButtonBase}" Content="{Binding Detach.Value, Source={x:Static ygo:Vocab.Current}}" Icon="{x:Static md:Icons.NewWindow}" Background="Transparent" HorizontalAlignment="Right" Click="OnClick_Detach" Visibility="{Binding CanDetach, Converter={StaticResource BVConv}}" Margin="1,3" Padding="3,0"/>
        </Grid>
        <!-- card info -->
        <Grid Grid.Row="1" ColumnDefinitions="*, 8, *" RowDefinitions="Auto, *">
            <!-- type and limit -->
            <Grid Grid.Row="0" Grid.Column="0" ColumnDefinitions="1*, 5*">
                <Label Content="{Binding CInfo.CardType.Value, Source={x:Static ygo:Vocab.Current}}"/>
                <Grid Grid.Column="1" ColumnDefinitions="Auto, *, Auto, Auto">
                    <ct:IconPresenter Grid.Column="0" Source="{Binding Card.Icon}" Margin="4,0,2,0"/>
                    <TextBlock Grid.Column="1" Text="{Binding Card.CardTypeText}" Margin="0"/>
                    <ct:IconPresenter Grid.Column="2" Margin="8,0,2,0" Source="{Binding Card.LimitIcon}"/>
                    <TextBlock Grid.Column="3" Margin="0,0,4,0" Text="{Binding Card.LimitText}"/>
                </Grid>
            </Grid>
            <!-- card text -->
            <Grid Grid.Row="0" Grid.Column="2" ColumnDefinitions="Auto, *, Auto">
                <Label Grid.Column="0" Content="{Binding CInfo.Text.Value, Source={x:Static ygo:Vocab.Current}}"/>
                <ct:Button Style="{StaticResource CopyButton}" Tag="{Binding ElementName=TextBox_Text}"/>
                <TextBlock Grid.Column="2" FontSize="13" Margin="0" VerticalAlignment="Bottom">
                    <Run Text="{Binding CInfo.Id.Value, Source={x:Static ygo:Vocab.Current}, Mode=OneWay}"/>:<Run Text="{Binding Card.Id}"/>
                    <Hyperlink Focusable="False" Click="OnClick_DB1">
                        <Run Text="{Binding OfficialDatabase.Value, Source={x:Static ygo:Vocab.Current}, Mode=OneWay}"/>
                    </Hyperlink>
                    <Hyperlink Focusable="False" Click="OnClick_DB2">
                        <Run Text="{Binding TcgDatabase.Value, Source={x:Static ygo:Vocab.Current}, Mode=OneWay}"/>
                    </Hyperlink>
                </TextBlock>
            </Grid>
            <TextBox Grid.Row="1" Grid.Column="2" x:Name="TextBox_Text" Style="{StaticResource TextBox_Text}" Text="{Binding Card.Text, Mode=OneWay, Converter={StaticResource LFConv}}"/>
            <TextBlock Grid.Row="1" Grid.Column="2" Style="{StaticResource TextBlock_TextLength}" Text="{Binding Text, ElementName=TextBox_Text, Converter={StaticResource TextLengthConverter}}"/>
            <!-- monster info -->
            <Grid Grid.Row="1" Grid.Column="0" RowDefinitions="Auto, *">
                <Grid Grid.Row="0" RowDefinitions="Auto, Auto, Auto" Visibility="{Binding HasMonsterInfo, Converter={StaticResource BVConv}}">
                    <Grid Grid.Row="0" ColumnDefinitions="*, 2*, *, 2*">
                        <Label Grid.Column="0" Content="{Binding CInfo.Attribute.Value, Source={x:Static ygo:Vocab.Current}}"/>
                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <ct:IconPresenter Source="{Binding Card.AttributeIcon}" Margin="4,0,2,0"/>
                            <TextBlock Text="{Binding Card.AttributeText}" Margin="0"/>
                        </StackPanel>
                        <Label Grid.Column="2" Content="{Binding CInfo.MonsterType.Value, Source={x:Static ygo:Vocab.Current}}"/>
                        <TextBlock Grid.Column="3" Text="{Binding Card.MonsterTypeText}"/>
                    </Grid>
                    <Grid Grid.Row="1" ColumnDefinitions="*, 5*">
                        <Label Grid.Column="0" Content="{Binding CInfo.Ability.Value, Source={x:Static ygo:Vocab.Current}}"/>
                        <TextBlock Grid.Column="1" Text="{Binding Card.AbilityTextWithType}"/>
                    </Grid>
                    <Grid Grid.Row="2" ColumnDefinitions="*, *, *, *, 2*">
                        <Label Grid.Column="0" Content="{Binding LevelName}"/>
                        <TextBlock Grid.Column="1" Text="{Binding Card.LevelText}"/>
                        <Label Grid.Column="2" Content="{Binding CInfo.Atk.Value, Source={x:Static ygo:Vocab.Current}}"/>
                        <TextBlock Grid.Column="3" Text="{Binding Card.AtkText}"/>
                        <Grid Grid.Column="4" ColumnDefinitions="*, *" Visibility="{Binding IsLevelEnabled, Converter={StaticResource BVConv}}">
                            <Label Grid.Column="0" Content="{Binding CInfo.Def.Value, Source={x:Static ygo:Vocab.Current}}"/>
                            <TextBlock Grid.Column="1" Text="{Binding Card.DefText}"/>
                        </Grid>
                        <Grid Grid.Column="4" ColumnDefinitions="1.25*, 0.75*" Visibility="{Binding IsLevelEnabled, Converter={StaticResource TVConvI}}">
                            <Label Grid.Column="0" Content="{Binding CInfo.LinkMarker.Value, Source={x:Static ygo:Vocab.Current}}"/>
                            <local:LinkMarkerView Grid.Column="1" IsReadOnly="True" Direction="{Binding LinkDirections, Mode=OneWay}" Width="{Binding ActualHeight, RelativeSource={RelativeSource Self}}"/>
                        </Grid>
                    </Grid>
                </Grid>
                <!-- pendulum info -->
                <Grid Grid.Row="1" RowDefinitions="Auto, *" Visibility="{Binding HasPendulumInfo, Converter={StaticResource BVConv}}">
                    <Grid Grid.Row="0" ColumnDefinitions="*, 2*, 2*, *">
                        <Label Content="{Binding CInfo.PendulumText.Value, Source={x:Static ygo:Vocab.Current}}"/>
                        <ct:Button Style="{StaticResource CopyButton}" Tag="{Binding ElementName=TextBox_PText}"/>
                        <Label Grid.Column="2" Content="{Binding CInfo.PendulumScale.Value, Source={x:Static ygo:Vocab.Current}}"/>
                        <TextBlock Grid.Column="3" Text="{Binding Card.PendulumScale}"/>
                    </Grid>
                    <TextBox Grid.Row="1" Grid.Column="2" x:Name="TextBox_PText" Style="{StaticResource TextBox_Text}" Text="{Binding Card.PendulumText, Mode=OneWay, Converter={StaticResource LFConv}}"/>
                    <TextBlock Grid.Row="1" Grid.Column="2" Style="{StaticResource TextBlock_TextLength}" Text="{Binding Text, ElementName=TextBox_PText, Converter={StaticResource TextLengthConverter}}"/>
                </Grid>
            </Grid>
        </Grid>
        <!-- pack info -->
        <Grid Grid.Row="1" Grid.RowSpan="3" ColumnDefinitions="*, 8, *" RowDefinitions="Auto, *" VerticalAlignment="Bottom">
            <Expander x:Name="Expander_PackList" ExpandDirection="Up" Background="White" Padding="0" BorderThickness="0">
                <Expander.Header>
                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <StackPanel.Resources>
                            <Style TargetType="TextBlock">
                                <Setter Property="Margin" Value="1,0"/>
                            </Style>
                        </StackPanel.Resources>
                        <TextBlock Text="{Binding CInfo.PackInfo.Value, Source={x:Static ygo:Vocab.Current}}" Margin="0,0,1,0"/>
                        <TextBlock Text="("/>
                        <TextBlock Text="{Binding Card.PackInfo.OcgCount}"/>
                        <TextBlock Text="+"/>
                        <TextBlock Text="{Binding Card.PackInfo.TcgCount}"/>
                        <TextBlock Text="="/>
                        <TextBlock Text="{Binding Card.PackInfo.Count}"/>
                        <TextBlock Text=")"/>
                    </StackPanel>
                </Expander.Header>
            </Expander>
            <ListBox Grid.Column="0" Style="{StaticResource ListBox_Link}" ItemsSource="{Binding Card.PackInfo}" Height="{Binding IsExpanded, ElementName=Expander_PackList, Converter={StaticResource ExpConv}}">
                <ListBox.ItemTemplate>
                    <DataTemplate DataType="ygd:PackInfo">
                        <Grid Background="Transparent" ColumnDefinitions="Auto, 4, Auto, 4, *">
                            <TextBlock Grid.Column="0" Text="{Binding DateText}" Margin="0"/>
                            <TextBlock Grid.Column="2" Text="{Binding Number}" FontFamily="Consolas" FontSize="9" VerticalAlignment="Bottom" Margin="0,0,0,1" />
                            <TextBlock Grid.Column="4" x:Name="Link" Margin="0">
                                <Hyperlink Focusable="False" Click="OnClick_Pack">
                                    <Run Text="{Binding Name, Mode=OneWay}"/>
                                </Hyperlink>
                            </TextBlock>
                            <Popup x:Name="Popup" IsOpen="False" PlacementTarget="{Binding ElementName=Link}">
                                <Border Style="{StaticResource HelpPopupContent}">
                                    <TextBlock Text="{Binding Name}"/>
                                </Border>
                            </Popup>
                        </Grid>
                        <DataTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True" SourceName="Link">
                                <Setter TargetName="Popup" Property="IsOpen" Value="True"/>
                            </Trigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
        <!-- relative info -->
        <Grid Grid.Row="3" ColumnDefinitions="*, 8, *" RowDefinitions="Auto, *">
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <StackPanel.Resources>
                    <Style TargetType="TextBlock">
                        <Setter Property="Margin" Value="1,0"/>
                    </Style>
                </StackPanel.Resources>
                <TextBlock Text="{Binding CInfo.RelatedList.Value, Source={x:Static ygo:Vocab.Current}}" Margin="4,0,1,0"/>
                <TextBlock Text="("/>
                <TextBlock Text="{Binding RelatedTexts.Count}"/>
                <TextBlock Text=")"/>
            </StackPanel>
            <ListBox Grid.Column="2" Style="{StaticResource ListBox_Link}" ItemsSource="{Binding RelatedTexts}">
                <ListBox.ItemTemplate>
                    <DataTemplate DataType="string">
                        <Grid Background="Transparent">
                            <TextBlock x:Name="Link" Margin="0">
                                <Hyperlink Focusable="False" Click="OnClick_RelatedText">
                                    <Run Text="{Binding Mode=OneWay}"/>
                                </Hyperlink>
                            </TextBlock>
                            <Popup x:Name="Popup" IsOpen="False" PlacementTarget="{Binding ElementName=Link}">
                                <Border Style="{StaticResource HelpPopupContent}">
                                    <TextBlock Text="{Binding}"/>
                                </Border>
                            </Popup>
                        </Grid>
                        <DataTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True" SourceName="Link">
                                <Setter TargetName="Popup" Property="IsOpen" Value="True"/>
                            </Trigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Grid>
</UserControl>
