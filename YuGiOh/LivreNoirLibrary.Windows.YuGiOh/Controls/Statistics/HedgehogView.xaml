<UserControl x:Class="LivreNoirLibrary.Windows.YuGiOh.Controls.HedgehogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LivreNoirLibrary.Windows.YuGiOh.Controls"
             xmlns:ct="clr-namespace:LivreNoirLibrary.Windows.Controls;assembly=LivreNoirLibrary.Windows.Controls"
             xmlns:media="clr-namespace:LivreNoirLibrary.Windows.Media;assembly=LivreNoirLibrary.Windows.Media"
             xmlns:yw="clr-namespace:LivreNoirLibrary.Windows.YuGiOh;assembly=LivreNoirLibrary.Windows.YuGiOh"
             xmlns:ygo="clr-namespace:LivreNoirLibrary.YuGiOh.Data;assembly=LivreNoirLibrary.YuGiOh.Core"
             xmlns:input="clr-namespace:LivreNoirLibrary.Windows.Input;assembly=LivreNoirLibrary.Windows.Core"
             mc:Ignorable="d" Background="White"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/LivreNoirLibrary.Windows.YuGiOh;component/Themes/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <Style TargetType="TextBlock">
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>
            <Style TargetType="GridViewColumnHeader">
                <EventSetter Event="Click" Handler="OnClick_GridViewColumnHeader"/>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid x:Name="MainGrid" ColumnDefinitions="2*, Auto, 3*, 4, 3*" RowDefinitions="Auto, *" d:DataContext="{d:DesignInstance Type=local:HedgehogView}">
        <Grid ColumnDefinitions="Auto, *, Auto" Margin="0,0,0,4">
            <ct:Button Style="{StaticResource RefreshButton}"/>
            <ct:LabeledSlider Grid.Column="1" Margin="4,0" Header="★" Minimum="1" Maximum="12" TickFrequency="1" Value="{Binding LevelLimit}"/>
            <TextBlock Grid.Column="2" Text="{Binding LessOrEqual.Value, Source={x:Static yw:Vocab.Current}}"/>
        </Grid>
        <ListView Grid.Row="2" x:Name="ListView_Main" Style="{StaticResource Alt2ListBoxStyle}" SelectionMode="Single" SelectedItem="{Binding SelectedItem, Mode=TwoWay}">
            <ListView.View>
                <GridView>
                    <GridViewColumn HeaderTemplate="{StaticResource GridViewHeader_Level}" CellTemplate="{StaticResource ItemTemplate_Level}"/>
                    <GridViewColumn HeaderTemplate="{StaticResource GridViewHeader_Attribute}" CellTemplate="{StaticResource ItemTemplate_Attribute}"/>
                    <GridViewColumn HeaderTemplate="{StaticResource GridViewHeader_MonsterType}" CellTemplate="{StaticResource ItemTemplate_MonsterType}"/>
                    <GridViewColumn>
                        <GridViewColumn.HeaderTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding CType.Normal.Value, Source={x:Static yw:Vocab.Current}}" Tag="NormalCount"/>
                            </DataTemplate>
                        </GridViewColumn.HeaderTemplate>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding NormalCount}" HorizontalAlignment="Right"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn>
                        <GridViewColumn.HeaderTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding CType.Effect.Value, Source={x:Static yw:Vocab.Current}}" Tag="EffectCount"/>
                            </DataTemplate>
                        </GridViewColumn.HeaderTemplate>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding EffectCount}" HorizontalAlignment="Right"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
        <TextBlock Style="{StaticResource TextBlock_RightArrow}"/>
        <StackPanel Grid.Column="2" Orientation="Horizontal">
            <Label Style="{StaticResource Label_Header}" Content="{Binding CType.Normal_Monster.Value, Source={x:Static yw:Vocab.Current}}"/>
            <TextBlock Margin="4,0" Text="{Binding Items.Count, ElementName=ListView_Normal}"/>
        </StackPanel>
        <ListView Grid.Column="2" Grid.Row="2" x:Name="ListView_Normal" Style="{StaticResource CardListView}" ItemsSource="{Binding SelectedItem.NormalMonsters}">
            <ListView.View>
                <GridView>
                    <GridViewColumn HeaderTemplate="{StaticResource GridViewHeader_Name}" CellTemplate="{StaticResource ItemTemplate_NameWithLink}"/>
                    <GridViewColumn Width="48" HeaderTemplate="{StaticResource GridViewHeader_Atk}" CellTemplate="{StaticResource ItemTemplate_Atk}"/>
                    <GridViewColumn Width="48" HeaderTemplate="{StaticResource GridViewHeader_Def}" CellTemplate="{StaticResource ItemTemplate_Def}"/>
                    <GridViewColumn Width="26" HeaderTemplate="{StaticResource GridViewHeader_Tuner}" CellTemplate="{StaticResource ItemTemplate_Tuner}"/>
                    <GridViewColumn HeaderTemplate="{StaticResource GridViewHeader_Details}" CellTemplate="{StaticResource ItemTemplate_Detail}"/>
                </GridView>
            </ListView.View>
        </ListView>
        <StackPanel Grid.Column="4" Orientation="Horizontal">
            <Label Style="{StaticResource Label_Header}" Content="{Binding CType.Effect_Monster.Value, Source={x:Static yw:Vocab.Current}}"/>
            <TextBlock Margin="4,0" Text="{Binding Items.Count, ElementName=ListView_Effect}"/>
        </StackPanel>
        <ListView Grid.Column="4" Grid.Row="2" x:Name="ListView_Effect" Style="{StaticResource CardListView}" ItemsSource="{Binding SelectedItem.EffectMonsters}">
            <ListView.View>
                <GridView>
                    <GridViewColumn HeaderTemplate="{StaticResource GridViewHeader_Name}" CellTemplate="{StaticResource ItemTemplate_NameWithLink}"/>
                    <GridViewColumn Width="48" HeaderTemplate="{StaticResource GridViewHeader_Atk}" CellTemplate="{StaticResource ItemTemplate_Atk}"/>
                    <GridViewColumn Width="48" HeaderTemplate="{StaticResource GridViewHeader_Def}" CellTemplate="{StaticResource ItemTemplate_Def}"/>
                    <GridViewColumn Width="26" HeaderTemplate="{StaticResource GridViewHeader_Tuner}" CellTemplate="{StaticResource ItemTemplate_Tuner}"/>
                    <GridViewColumn HeaderTemplate="{StaticResource GridViewHeader_Details}" CellTemplate="{StaticResource ItemTemplate_Detail}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
